<h2>Dogs</h2>
<div>
    <% @dogs.each do |dog| %>
        <div><p><%= link_to dog.name, dog_path(dog) %></p></div>
    <% end %>
    <div><%= button_to "Add a New Dog", new_dog_path, method: :get %></div>
</div>